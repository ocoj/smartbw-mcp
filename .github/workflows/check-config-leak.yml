name: Config Leak Check

on: [push, pull_request]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 扫描真实配置文件
        run: bash scripts/pre-commit.sh
